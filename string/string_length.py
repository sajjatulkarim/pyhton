# string length using len() function, which returns the number of characters in a string
my_string = "Hello, World!"
length = len(my_string) # length will be 13
print("Length of the string is:", length)